# Examples

Local working directory for showcase data and generated lake artifacts
(fixture MCAP files, `*.lance` lakes, exported clips). Contents are
gitignored; only this README is tracked.

The baseline ingest showcase is expected to run from here, e.g.:

```bash
uv run lancedb-robotics lake init --lake examples/demo.robot.lance
```
